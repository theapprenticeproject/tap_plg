# Testing dependencies for MentorMe plagiarism detection system

# Core testing framework
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0
pytest-mock==3.14.0

# Test fixtures and data generation
faker==33.3.0

# Code quality and linting
ruff==0.9.1  # Modern replacement for flake8, isort, black (faster, all-in-one)
mypy==1.14.1

# Coverage reporting
coverage[toml]==7.6.10

# Performance testing
pytest-benchmark==5.1.0
locust==2.32.4

# Mocking HTTP requests
responses==0.25.3
aioresponses==0.7.8

# Type stubs for better mypy checking
types-requests==2.32.0.20241016
types-pillow==10.2.0.20240822

# All production dependencies
-r requirements.txt
